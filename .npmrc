# SPDX-FileCopyrightText: 2026 CABOC contributors
# SPDX-License-Identifier: Apache-2.0

# pnpm + npm config shared by every contributor.
# This file is committed. Never put auth tokens here.
# Local auth lives in ~/.npmrc or CI secrets (NPM_TOKEN).

# --- registry ---
registry=https://registry.npmjs.org/
@caboc:registry=https://registry.npmjs.org/

# --- auth (env-var reference; literal token NEVER stored here) ---
# Requires NPM_TOKEN to be exported in the publish shell or CI environment.
# Generate token at https://www.npmjs.com/settings/<user>/tokens with
# Automation or Granular type scoped to @caboc/*.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# --- engine + package manager enforcement ---
engine-strict=true
package-manager-strict=true

# --- workspace resolution (pnpm) ---
link-workspace-packages=true
prefer-workspace-packages=true
resolve-peers-from-workspace-root=true
auto-install-peers=true
strict-peer-dependencies=false

# --- version pinning ---
save-exact=false
save-prefix=^
resolution-mode=highest

# --- safety + signing ---
audit-level=high
fund=false
update-notifier=false
provenance=true

# --- script discipline ---
enable-pre-post-scripts=true
ignore-scripts=false

# --- logging ---
loglevel=warn

# --- publish defaults for scoped packages ---
access=public
tag=next

# --- editorial ---
git-tag-version=false
commit-hooks=false
